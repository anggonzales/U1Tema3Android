<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.u1tema3android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ejemplo3.MainFragmentCorreo"></activity>
        <activity android:name=".Ejemplo2.detalle_correo" />
        <activity android:name=".lis_item_correo" />
        <activity android:name=".Ejemplo2.ListaCorreo" />
        <activity android:name=".Ejemplo1.MainActivity" />
        <activity android:name=".Ejemplo1.grid_fragmento" />
        <activity android:name=".Ejemplo1.lista_fragmento" />
        <activity android:name=".EjemplosFragment">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>